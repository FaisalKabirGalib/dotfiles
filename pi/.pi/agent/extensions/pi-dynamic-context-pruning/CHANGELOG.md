# Changelog — Personal Fork

All changes on top of upstream [complexthings/pi-dynamic-context-pruning](https://github.com/complexthings/pi-dynamic-context-pruning).

## Unreleased

- Fixed type error in `config.ts` (`unknown[]` → `any[]` for jsonc-parser)
- Added `tsconfig.json` for local type checking
- Added dev dependencies: `typescript`, `@mariozechner/pi-coding-agent`, `@mariozechner/pi-tui`
- Rewrote README and AGENTS.md for personal dotfiles context
