# System packages for pacman
# One package per line, comments allowed

base-devel
git
curl
wget
unzip
zip
neovim
tmux
zsh
fzf
fd
ripgrep
bat
eza
zoxide
python
hyprland
waybar
rofi
wl-clipboard
cliphist

# Toolchain manager. Owns language runtimes (node, bun, python, go, rust, java,
# ruby, flutter) and dev CLIs (just, shellcheck, shfmt, gitleaks, terraform, ...)
# — see mise/PACKAGE.md. Deliberately NOT duplicated here as pacman packages.
mise

# Symlink manager. Stays on pacman: no mise registry entry, and it is needed
# before ~/.config/mise/config.toml can be stowed into place.
stow

# Desktop rice: notification center, media control for waybar, audio visualizer
swaync
playerctl
cava

# Media + the `yt` shell function (zsh/.config/zsh/60-functions.zsh):
# yt-dlp fetches, mpv streams, chafa renders thumbnails in the fzf picker.
# intel-media-driver provides VA-API hardware decode for Iris Xe — see mpv/PACKAGE.md.
mpv
yt-dlp
chafa
intel-media-driver

# Wireless Android screen mirroring via the `mirror` alias / scrcpy-wireless.
# android-tools supplies adb.
scrcpy
android-tools
