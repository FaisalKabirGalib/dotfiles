# pj

Project discovery configuration for the `pj` CLI and the Herdr PJ picker.

## Install

```bash
./stowup pj
```

The project roots match the existing `hp` picker: `~/personal`, `~/work`, and
`~/Dev`.
