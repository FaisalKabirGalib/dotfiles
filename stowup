#!/usr/bin/env bash

set -euo pipefail

echo "stowing dotfiles....."

for pkg in */; do
	pkg="${pkg%/}"
	[ -f "$pkg/PACKAGE.md" ] || continue
	echo "stowing $pkg"
	stow -D "$pkg" 2>/dev/null || true
	stow "$pkg"
done
