#!/usr/bin/env bash
set -euo pipefail

source "$(dirname -- "${BASH_SOURCE[0]}")/scripts/stow-common.sh"

if [[ "$#" -gt 0 ]]; then
	for package in "$@"; do
		stow_package unstow "$package"
	done
else
	stow_all_packages unstow
fi
