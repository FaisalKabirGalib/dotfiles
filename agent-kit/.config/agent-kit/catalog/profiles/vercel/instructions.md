# Vercel Profile

Use this profile only when the project is intentionally deployed on Vercel. Use Vercel MCP with OAuth only for the current account and project, and keep deployment, domain, and log actions scoped to the requested environment.

This profile does not change the project's database choice. A Next.js project may continue to use its own PostgreSQL database and Docker-based deployment unless the project explicitly adopts Vercel.
