# Expo Profile

Use Expo Skills and Expo MCP only for Expo projects. Follow the installed Expo SDK and use `npx expo install` for compatible dependencies. Keep project context files and app configuration aligned with the installed SDK.

Use the Expo MCP only for the current project, account, builds, and simulator. Verify UI changes on a local simulator or device when available; do not access unrelated EAS projects or production credentials.
