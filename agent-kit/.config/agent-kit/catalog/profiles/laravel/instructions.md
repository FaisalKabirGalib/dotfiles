# Laravel Profile

Use Laravel Boost inside Laravel projects for Laravel-specific guidance, agent skills, and documentation MCP access. Preserve the application's conventions for Eloquent, validation, authorization, queues, events, tests, and frontend integration.

Use the `docker` profile only when Sail, Compose, a Dockerfile, or a devcontainer is already present. Do not introduce a database provider or deployment platform without a project requirement.
