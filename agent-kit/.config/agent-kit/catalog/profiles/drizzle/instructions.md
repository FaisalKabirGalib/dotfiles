# Drizzle Profile

Use Drizzle only when the repository already uses it or it has been selected for the project. Keep schema definitions, migrations, and queries SQL-aware and type-safe. Run the narrowest generation, validation, and migration-status checks available before proposing an apply step.

Use `drizzle-kit mcp` and bundled Drizzle skills only for this project. Never apply destructive migrations or connect to production without explicit confirmation.
