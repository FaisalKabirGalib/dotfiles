# Ghostty Terminal Configuration
# Migrated from Alacritty setup with Catppuccin Mocha theme

# Font Configuration
font-family = Maple Mono NF
font-size = 16

# Window Configuration
window-padding-x = 14
window-padding-y = 14
window-decoration = false
background-opacity = 0.80

# Shell Configuration
command = /usr/bin/zsh -l

# Terminal Environment
term = xterm-256color

# Keybindings
keybind = f11=toggle_fullscreen

# Basic Performance Optimizations
confirm-close-surface = false
mouse-hide-while-typing = true

# Clipboard Integration
copy-on-select = false

# Scrollback
scrollback-limit = 10000

# Window Controls
quit-after-last-window-closed = true

# Shell Integration
shell-integration = detect

# Colors: inherit entirely from the active Omarchy theme so the terminal
# tracks `omarchy theme set` automatically (no hardcoded palette overrides).
theme = "/home/galib/.config/omarchy/current/theme/ghostty.conf"

# Cursor (style only — color is derived from the theme)
cursor-style = block
cursor-style-blink = false

# Slowdown mouse scrolling
mouse-scroll-multiplier = 0.95

# Fix general slowness on hyprland (https://github.com/ghostty-org/ghostty/discussions/3224)
async-backend = epoll
